target/

# circomlib sources fetched by scripts/fetch_circomlib.sh
circom-libs/

# Circom / Groth16 build outputs (regenerated by benches or Docker)
**/generated/

# Benchmark run artifacts (summaries are optional to commit; per-iter dirs are not)
artifacts_bench_*/
**/artifacts_bench_*/
**/artifacts_measure_communication_size/

# Local ptau download (Dockerfile COPY expects the file at build time)
powersOfTau/*.ptau

# Circuits generated by the merkle-vs-flat sweep
merkle-vs-flat/flat_t*_u*.circom
merkle-vs-flat/merkle_t*_u*.circom

# Docker reproduction summaries
results/
